function handleChat(channel, user, message, self) {
  if (!document.hidden)
    if (length) {
      if (message.length <= length)
        newChat.push(message);
    } else
      newChat.push(message);
}
